# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.



# Tienda Artesanal - Project Break

Tienda online de productos artesanales hecha con React, Node y MongoDB.

## Frontend
- React + CSS Modules
- Rutas protegidas
- Context para carrito

## Backend
- Node.js + Express
- MongoDB

## Cómo ejecutar
1. Instala dependencias con `npm install` en cada carpeta (`Back`, `Front`).
2. Arranca el backend:  
3. Arranca el frontend:
4. Abre el navegador en `http://localhost:5173`.

---

## 3️⃣ **¿Cómo desplegar?**

### **Frontend (React)**
- **Netlify** (el más fácil: sube tu carpeta Front como un proyecto nuevo).
- **Vercel** (también muy fácil).

**¿Quieres guía rápida para Netlify o Vercel?**

---

### **Backend (Node.js)**
- **Railway** o **Render.com** (son gratis y sencillos para Express/MongoDB).

**¿Quieres guía rápida para Railway o Render?**

---

**¡Dime si quieres los pasos detallados para Netlify y/o Railway!  
O si tienes preferencia por alguna otra plataforma.**

¿Te hago un ejemplo de `.env` y README para copiar/pegar?
